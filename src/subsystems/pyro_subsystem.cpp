#include "pyro_subsystem.hpp"
#include "state.hpp"

/// @brief tests if the pyro acted on the derived state
/// @return true if the pyro subsystem was able to act on the derived state, false otherwise 
bool PyroSubsystem::success() {
    // TODO
}
/// @brief makes the pyro take action based on the derived state passed
/// @param state the derived state to act on
void PyroSubsystem::apply_state(DerivedState &state) {
    // TODO
}